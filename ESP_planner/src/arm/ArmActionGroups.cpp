/**
 * @file ArmActionGroups.cpp
 * @brief 机械臂预设动作组数据定义
 *
 * 所有动作组的 PWM 参数集中在此文件，便于后续现场调参。
 *
 * 重要说明：
 * 1. 以下 PWM 值为安全示例值（1500 中位），实际值需要连接舵机后现场标定。
 * 2. 每个动作组的步骤数和每步参数都需要根据实际机械臂调整。
 * 3. 修改时只需改本文件，业务代码自动同步。
 *
 * 调参步骤建议：
 * 1. 先用 BusServoDriver::moveServo() 单独控制每个舵机，确定各关节的安全范围
 * 2. 确定夹取位置、放下位置、复位位置对应的 PWM 值
 * 3. 确定合适的运动时间（T 值），太快可能抖动，太慢效率低
 * 4. 确定步骤间的等待时间（waitMs）
 *
 * PWM 参考范围：
 *   0500 = 一个极限方向
 *   1500 = 中位
 *   2500 = 另一个极限方向
 */

#include "arm/ArmActionGroups.h"

// ============================================================
// 动作组数据
// ============================================================
const ActionGroup actionGroups[] = {

    // --------------------------------------------------------
    // ACTION 0: 双臂复位 (HOME)
    // 所有舵机回到中位安全位置
    // --------------------------------------------------------
    {
        "HOME",
        {
            // 步骤1：8个舵机同时回到中位，耗时1500ms
            {{
                {LEFT_BASE_ID,     1500, 1500},
                {LEFT_SHOULDER_ID, 1500, 1500},
                {LEFT_ELBOW_ID,    1500, 1500},
                {LEFT_GRIPPER_ID,  1500, 1500},  // 夹爪张开
                {RIGHT_BASE_ID,     1500, 1500},
                {RIGHT_SHOULDER_ID, 1500, 1500},
                {RIGHT_ELBOW_ID,    1500, 1500},
                {RIGHT_GRIPPER_ID,  1500, 1500},  // 夹爪张开
            }, 8, 500},
        },
        1
    },

    // --------------------------------------------------------
    // ACTION 1: 左臂夹取 (LEFT_PICK)
    // 左臂移动到夹取位置并闭合夹爪
    // --------------------------------------------------------
    {
        "LEFT_PICK",
        {
            // 步骤1：左臂移动到预夹取位置（底座+肩+肘），夹爪张开
            {{
                {LEFT_BASE_ID,     1500, 1000},  // TODO: 实际角度待标定
                {LEFT_SHOULDER_ID, 1300, 1000},  // TODO: 实际角度待标定
                {LEFT_ELBOW_ID,    1700, 1000},  // TODO: 实际角度待标定
                {LEFT_GRIPPER_ID,  1500, 500},   // 夹爪张开
            }, 4, 1200},  // 等待动作完成+稳定

            // 步骤2：闭合夹爪抓取
            {{
                {LEFT_GRIPPER_ID,  2000, 800},   // TODO: 夹紧 PWM 待标定
            }, 1, 1000},
        },
        2
    },

    // --------------------------------------------------------
    // ACTION 2: 左臂放下 (LEFT_PLACE)
    // 左臂移动到放置位置并张开夹爪
    // --------------------------------------------------------
    {
        "LEFT_PLACE",
        {
            // 步骤1：左臂移动到放置位置
            {{
                {LEFT_BASE_ID,     1500, 1000},  // TODO: 实际角度待标定
                {LEFT_SHOULDER_ID, 1700, 1000},  // TODO: 实际角度待标定
                {LEFT_ELBOW_ID,    1300, 1000},  // TODO: 实际角度待标定
            }, 3, 1200},

            // 步骤2：张开夹爪放下货物
            {{
                {LEFT_GRIPPER_ID,  1500, 800},   // 夹爪张开
            }, 1, 1000},
        },
        2
    },

    // --------------------------------------------------------
    // ACTION 3: 右臂夹取 (RIGHT_PICK)
    // --------------------------------------------------------
    {
        "RIGHT_PICK",
        {
            // 步骤1：右臂移动到预夹取位置
            {{
                {RIGHT_BASE_ID,     1500, 1000},  // TODO: 实际角度待标定
                {RIGHT_SHOULDER_ID, 1700, 1000},  // TODO: 实际角度待标定
                {RIGHT_ELBOW_ID,    1300, 1000},  // TODO: 实际角度待标定
                {RIGHT_GRIPPER_ID,  1500, 500},   // 夹爪张开
            }, 4, 1200},

            // 步骤2：闭合夹爪抓取
            {{
                {RIGHT_GRIPPER_ID,  2000, 800},   // TODO: 夹紧 PWM 待标定
            }, 1, 1000},
        },
        2
    },

    // --------------------------------------------------------
    // ACTION 4: 右臂放下 (RIGHT_PLACE)
    // --------------------------------------------------------
    {
        "RIGHT_PLACE",
        {
            // 步骤1：右臂移动到放置位置
            {{
                {RIGHT_BASE_ID,     1500, 1000},  // TODO: 实际角度待标定
                {RIGHT_SHOULDER_ID, 1300, 1000},  // TODO: 实际角度待标定
                {RIGHT_ELBOW_ID,    1700, 1000},  // TODO: 实际角度待标定
            }, 3, 1200},

            // 步骤2：张开夹爪放下货物
            {{
                {RIGHT_GRIPPER_ID,  1500, 800},   // 夹爪张开
            }, 1, 1000},
        },
        2
    },

    // --------------------------------------------------------
    // ACTION 5: 双臂夹取 (BOTH_PICK)
    // 左右臂同时执行夹取动作
    // --------------------------------------------------------
    {
        "BOTH_PICK",
        {
            // 步骤1：双臂移动到预夹取位置
            {{
                {LEFT_BASE_ID,      1500, 1000},
                {LEFT_SHOULDER_ID,  1300, 1000},
                {LEFT_ELBOW_ID,     1700, 1000},
                {LEFT_GRIPPER_ID,   1500, 500},
                {RIGHT_BASE_ID,     1500, 1000},
                {RIGHT_SHOULDER_ID, 1700, 1000},
                {RIGHT_ELBOW_ID,    1300, 1000},
                {RIGHT_GRIPPER_ID,  1500, 500},
            }, 8, 1200},

            // 步骤2：闭合夹爪
            {{
                {LEFT_GRIPPER_ID,   2000, 800},
                {RIGHT_GRIPPER_ID,  2000, 800},
            }, 2, 1000},
        },
        2
    },

    // --------------------------------------------------------
    // ACTION 6: 双臂放下 (BOTH_PLACE)
    // --------------------------------------------------------
    {
        "BOTH_PLACE",
        {
            // 步骤1：双臂移动到放置位置
            {{
                {LEFT_BASE_ID,      1500, 1000},
                {LEFT_SHOULDER_ID,  1700, 1000},
                {LEFT_ELBOW_ID,     1300, 1000},
                {RIGHT_BASE_ID,     1500, 1000},
                {RIGHT_SHOULDER_ID, 1300, 1000},
                {RIGHT_ELBOW_ID,    1700, 1000},
            }, 6, 1200},

            // 步骤2：张开夹爪
            {{
                {LEFT_GRIPPER_ID,   1500, 800},
                {RIGHT_GRIPPER_ID,  1500, 800},
            }, 2, 1000},
        },
        2
    },

    // --------------------------------------------------------
    // ACTION 7: 停止所有舵机 (STOP_ALL)
    // 发送停止指令，舵机保持当前位置
    // --------------------------------------------------------
    {
        "STOP_ALL",
        {
            // 此动作组无步骤，由 ArmController 直接调用 BusServoDriver::stopAll()
            {{}, 0, 0},
        },
        0
    },

    // --------------------------------------------------------
    // ACTION 8: 急停 (EMERGENCY_STOP)
    // 先停止再释力
    // --------------------------------------------------------
    {
        "EMERGENCY_STOP",
        {
            // 此动作组无步骤，由 ArmController 直接处理
            {{}, 0, 0},
        },
        0
    },

    // --------------------------------------------------------
    // ACTION 9: 释放所有舵机扭力 (TORQUE_OFF_ALL)
    // --------------------------------------------------------
    {
        "TORQUE_OFF_ALL",
        {
            {{}, 0, 0},
        },
        0
    },

    // --------------------------------------------------------
    // ACTION 10: 恢复所有舵机扭力 (TORQUE_ON_ALL)
    // --------------------------------------------------------
    {
        "TORQUE_ON_ALL",
        {
            {{}, 0, 0},
        },
        0
    },

}; // actionGroups[]
